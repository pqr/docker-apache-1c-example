# Для локального тестирования удобно использовать Vagrant
# vagrant up
# vagrant ssh

Vagrant.configure("2") do |config|

  # config.vm.box = "parallels/ubuntu-16.04"
  config.vm.box = "ubuntu/xenial64"

end
